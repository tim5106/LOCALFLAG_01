{
  "name": "local-flag",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev:web": "npm run dev --workspace @local-flag/web",
    "dev:api": "npm run dev --workspace @local-flag/api",
    "sync:tour-spots": "npm run sync:tour-spots --workspace @local-flag/api",
    "build": "npm run build --workspace @local-flag/web && npm run build --workspace @local-flag/api",
    "typecheck": "npm run typecheck --workspace @local-flag/web && npm run typecheck --workspace @local-flag/api",
    "contracts:check": "node scripts/validate-openapi.mjs",
    "test": "npm run contracts:check && npm run test --workspace @local-flag/web && npm run test --workspace @local-flag/api"
  },
  "devDependencies": {
    "yaml": "^2.9.0"
  }
}
